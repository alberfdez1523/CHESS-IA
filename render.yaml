services:
  - type: web
    name: chess-ia
    env: python
    plan: free
    buildCommand: set -e ; pip install -r requirements.txt ; cd engine/stockfish/src ; make -j2 build ARCH=x86-64
    startCommand: python server.py
    autoDeploy: true
