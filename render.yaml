services:
  - type: web
    name: gambito-dama-cuantico
    env: python
    plan: free
    buildCommand: set -e ; pip install -r requirements.txt ; cd frontend && npm install && npm run build
    startCommand: python server.py
    autoDeploy: true
